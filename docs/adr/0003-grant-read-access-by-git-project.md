# Grant repository read access by Git project

One Add Project consent grants RWL read access to the selected Git repository and all worktrees sharing its Git common directory. The grant follows Git project identity rather than individual directory paths, keeping worktree creation, rename, and supported moves frictionless while still requiring explicit consent before RWL reads a project.
