# Offer remote read during project registration

The Add Project consent presents remote Git and pull-request metadata access as a separate toggle that is enabled by default and can be declined or revoked independently. Remote synchronization writes only to RWL-owned storage, giving the common ahead/behind experience one-step setup without treating network access as inseparable from local project reading.
